robot: "go2"  # Robot name, "go2", "b2", "b2w", "h1", "go2w", "g1"
robot_scene: "scene.xml" # Robot scene, /unitree_robots/[robot]/scene.xml 

domain_id: 1  # Domain id, 0 for hardware, 1 for simulation
interface: "lo" # Interface, lo for localhost

use_joystick: 0 # Simulate Unitree WirelessController using a gamepad
joystick_type: "xbox" # support "xbox" and "switch" gamepad layout
joystick_device: "/dev/input/js0" # Device path
joystick_bits: 16 # Some game controllers may only have 8-bit accuracy

print_scene_information: 1 # Print link, joint and sensors information of robot

enable_elastic_band: 0 # Virtual spring band, used for lifting h1

camera_name: "" # Set to "head_camera" to enable video streaming
camera_width: 640
camera_height: 480
camera_fps: 30

depth_camera_name: "depth_camera" # Set to "depth_camera" to enable pointcloud publishing
depth_camera_width: 320
depth_camera_height: 240
depth_camera_fps: 10
depth_camera_stride: 4 # Sample every Nth pixel (1 = full, 4 = 16x fewer points)
pointcloud_topic: "rt/pointcloud"
